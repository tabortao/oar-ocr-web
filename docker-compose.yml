services:
  oar-ocr-web:
    build: .
    ports:
      - "3000:3000"
    volumes:
      # 持久化 OCR 请求日志
      - ./logs:/app/logs
    environment:
      - RUST_LOG=info
      - OAR_HOME=/app/models
      - PORT=3000
      - LOG_DIR=/app/logs
      - LOG_RETENTION_DAYS=30
    restart: unless-stopped
