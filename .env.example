# oar-ocr-web 配置
# 复制此文件为 .env 并修改

# API 认证 Token（可选，留空则不启用认证。含 $ 需用 $$ 转义）
TOKEN=I3M$$1@C7h9pXoX

# 服务端口（默认 3000）
PORT=3000

# 模型缓存目录（默认 ~/.oar）
OAR_HOME=

# OCR 请求日志目录（默认 ./logs）
LOG_DIR=./logs

# OCR 日志保留天数（默认 30，到期自动删除）
LOG_RETENTION_DAYS=30
